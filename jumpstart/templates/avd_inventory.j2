# This file is automatically generated via avd_inventory_builder.yml playbook
{{ baseline ['site_name'] }}:
  children:
    CVAAS:
      hosts:
        cvaas:
############################################
#           {{ baseline ['site_name'] }} CORE SWITCHES           #
############################################
    {{ baseline ['site_name'] }}_FABRIC:
      children:
        {{ baseline ['site_name'] }}_CORE:
          hosts:
{% for item in switches.list if item.os == 'eos' and 'CS' in item.hostname  %}
            {{ item.hostname }}:
              ansible_host: {{ item.mgmt_ip | ansible.utils.ipaddr('address') }}
              is_deployed: true
{% endfor %}
# ############################################
#           {{ baseline ['site_name'] }} ACCESS SWITCHES         #
############################################
        {{ baseline ['site_name'] }}_ACCESS:
          hosts:
{% for item in switches.list if item.os == 'eos' and 'DS' in item.hostname %}
            {{ item.hostname }}:
              ansible_host: {{ item.mgmt_ip | ansible.utils.ipaddr('address') }}
              is_deployed: true
{% endfor %}
############################################
#           {{ baseline ['site_name'] }} INET SWITCHES           #
############################################
        {{ baseline ['site_name'] }}_INET:
          hosts:
{%  for item in switches.list if item.os == 'eos' and 'INET' in item.hostname %}
            {{ item.hostname }}:
              ansible_host: {{ item.mgmt_ip | ansible.utils.ipaddr('address') }}
              is_deployed: true
{% endfor %}
############################################
#           {{ baseline ['site_name'] }} MGMT SWITCHES           #
############################################
        {{ baseline ['site_name'] }}_MGMT:
          hosts:
{%  for item in switches.list if item.os == 'eos' and 'MGMT' in item.hostname %}
            {{ item.hostname }}:
              ansible_host: {{ item.mgmt_ip | ansible.utils.ipaddr('address') }}
              is_deployed: true
{% endfor %}
############################################
#                Groups                    #
############################################
    {{ baseline ['site_name'] }}_FABRIC_SERVICES:
      children:
        {{ baseline ['site_name'] }}_CORE:
        {{ baseline ['site_name'] }}_ACCESS:
        {{ baseline ['site_name'] }}_INET:
        {{ baseline ['site_name'] }}_MGMT:
    {{ baseline ['site_name'] }}_FABRIC_PORTS:
      children:
        {{ baseline ['site_name'] }}_CORE:
        {{ baseline ['site_name'] }}_ACCESS:
        {{ baseline ['site_name'] }}_INET:
        {{ baseline ['site_name'] }}_MGMT:
